
  <!-- 🌟 Header/Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-info shadow">
    <div class="container ps-0 pe-0">
      <a class="navbar-brand fw-bold" href="#">MailX</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse justify-content-end" id="navbarContent">
        <ul class="navbar-nav mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" href="/">Home</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="#about-disposable-email">How It Works</a>
          </li>
        </ul>
        <div class="navbar-nav me-auto">
          <% if(typeof currentUser !=="undefined" && currentUser){ %>
            <a class="nav-link" href="/users/logout/<%= currentUser._id %>">Log out</a>
            <% } else { %>
              <a class="nav-link active" aria-current="page" href="/users/signup">SignUp</a>
              <a class="nav-link" href="/users/login">Log In</a>
              <% } %>
        </div>
         <!-- Optional Theme Toggle -->
         <button class="btn btn-outline-light ms-1" onclick="toggleTheme()">🌙 Dark Mode</button> 
      </div>
    </div>
  </nav>